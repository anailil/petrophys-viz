from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='sualization of the main geothermal targets in NL',
    author='lvm',
    license='MIT',
)
